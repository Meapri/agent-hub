---
name: grok-codex
description: "Use the Grok Codex leaf MCP for consent-gated xAI chat, model listing, and provider status."
---

# Grok Codex

1. Consent + `XAI_API_KEY`.
2. `grok_codex_chat` for direct Grok calls (default Chat Completions).
3. Use `api_mode=responses` only when needed for reasoning-oriented models.
4. Multi-step orchestration belongs in a separate orchestrator plugin.
